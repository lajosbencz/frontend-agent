---
permalink: /
---
<h1>{{ site.title }}</h1>
<ul class="posts">
{%- for post in collections.posts %}
<li>
  <a href="{{ post.url | url }}">{{ post.data.title }}</a>
  {%- if post.date %} <span class="meta">{{ post.date | isodate }}</span>{% endif %}
  <p>{{ post.data.description }}</p>
</li>
{%- else %}
<li><p>No posts yet.</p></li>
{%- endfor %}
</ul>
